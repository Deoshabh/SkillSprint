// This file (src/app/admin/course-designer/page.tsx)
// was a duplicate of src/app/(app)/admin/course-designer/page.tsx
// and caused a routing conflict ("two parallel pages that resolve to the same path").
// Its content has been replaced to resolve this error.
// The page for the /admin/course-designer route is now exclusively handled by
// src/app/(app)/admin/course-designer/page.tsx, which uses the main application layout.
export {}; // This ensures the file does not export a default component and is not treated as a page.

// This file (src/app/profile/page.tsx)
// is a placeholder to prevent routing conflicts with src/app/(app)/profile/page.tsx.
// The actual page content for the /profile route is handled by
// /src/app/(app)/profile/page.tsx, which uses the main application layout.
export {}; // This ensures the file does not export a default component and is not treated as a page.

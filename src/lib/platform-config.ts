/**
 * @fileOverview Platform-wide configurations.
 * These settings are currently hardcoded but are intended to be
 * configurable by administrators in future versions.
 */

/**
 * The maximum number of custom videos/playlists a user can add to a single module.
 */
export const USER_MODULE_VIDEO_LIMIT = 3;

// Add other platform-wide settings here in the future, e.g.:
// export const MAX_COURSES_PER_USER = 10;
// export const DEFAULT_AI_SUGGESTIONS = 5;
